{
  "name": "url-shortener-service",
  "version": "1.0.0",
  "description": "URL Shortener Service with Horizontal Scaling",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "test": "jest",
    "load-test": "npx artillery run load-test.yml",
    "docker:restart": "docker compose up -d --build && docker compose down",
    "docker:logs": "docker compose logs -f hash-lb-nginx",
    "docker:stats": "docker stats hash-lb-service-1 hash-lb-service-2 hash-lb-service-3 hash-lb-service-4 hash-lb-nginx"
  },
  "dependencies": {
    "axios": "^1.9.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.0.2"
  }
}
